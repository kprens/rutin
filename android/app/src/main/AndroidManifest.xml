<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ağ erişimi: Supabase, Sentry, AdMob ve IAP makbuz doğrulaması (http)
         için zorunlu. Bu satır olmadan release build'de tüm ağ çağrıları
         OS seviyesinde engellenir (debug/profile varyantları bunu ayrı
         manifest'lerinde zaten tanımlıyordu, release'de eksikti). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ (API 33+) çalışma zamanı bildirim izni: bu izin
         olmadan requestNotificationsPermission() (notifications.dart)
         sessizce başarısız olur ve hiçbir hatırlatıcı gösterilemez. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Rutin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Supabase OAuth (Google/Apple) geri dönüş linki. Tarayıcıda
                 girişi tamamlayan kullanıcı bu şema ile uygulamaya geri
                 döner. auth.dart'taki redirectTo değeriyle birebir eşleşmeli
                 ve Supabase Dashboard → Authentication → URL Configuration →
                 Redirect URLs listesine de eklenmelidir. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.alper.rutin" android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="${admobAppId}"/>

        <!-- Ana ekran widget'ı — bugünün alışkanlık listesi (bkz.
             HabitWidgetProvider.kt, lib/home_widget_service.dart). -->
        <receiver android:name=".HabitWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_widget_info"/>
        </receiver>
        <!-- NOT: home_widget paketinin arka plan (uygulama kapalıyken
             widget'a dokunma) callback receiver'ı kendi AndroidManifest.xml
             birleştirmesiyle (manifest merge) otomatik ekleniyor — burada
             elle tekrar tanımlanmadı (çakışma riski). -->
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
